package com.cdgeekcamp.redas.api.core.controller.json;

public class UrlToMq {
        private String url;

        public UrlToMq() {
        }

        public UrlToMq(String url) {
                this.url = url;
        }

        public String getUrl() {
                return url;
        }

        public void setUrl(String url) {
                this.url = url;
        }
}
